name: Cypress Tests

on:
  push:
    branches:
      - 'dev'

jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Install dep-es
        run: npm install
      - name: Cypress run
        run: npm run cypress-smoke:e2e
